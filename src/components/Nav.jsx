// Navbar.jsx
import React from "react";
import "./Home.css"; // Import the CSS file for styling

const Navbar = () => {
    return (
        <nav>
            <h1 className="navbar-title">Puppy Bowl</h1> {/* Add a class for styling */}
        </nav>
    );
};

export default Navbar;


